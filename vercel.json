{
    "buildCommand": "cd packages/control-center && npm run build",
    "outputDirectory": "packages/control-center/dist",
    "installCommand": "npm install",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}